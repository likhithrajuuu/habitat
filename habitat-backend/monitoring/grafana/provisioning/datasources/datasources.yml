apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: PostgreSQL
    type: postgres
    access: proxy
    url: host.docker.internal:5432
    database: habitat
    user: likhithrajup
    secureJsonData:
      password: likhithraju79
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    editable: false